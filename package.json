{
  "name": "awesome-baml-framework",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "baml-generate": "baml-cli generate",
    "build": "npm run baml-generate && tsc --build",
    "start": "npm run baml-generate && npx tsx src/main.ts",
    "eval": "npm run baml-generate && vitest run src/evals/",
    "eval:router": "npm run baml-generate && vitest run src/evals/router.eval.test.ts",
    "eval:scope": "npm run baml-generate && vitest run src/evals/scope.eval.test.ts",
    "eval:context": "npm run baml-generate && vitest run src/evals/context.eval.test.ts",
    "eval:tools": "npm run baml-generate && vitest run src/evals/tools.eval.test.ts",
    "test:baml": "baml-cli test",
    "precompute": "npx tsx scripts/precompute-embeddings.ts"
  },
  "devDependencies": {
    "@types/node": "^25.3.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "@boundaryml/baml": "^0.220.0",
    "dotenv": "^17.3.1"
  }
}
